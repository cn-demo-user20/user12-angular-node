apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: rest-jscreditscore-ing12
  annotations:
    kubernetes.io/ingress.class: 'nginx'
    ingress.kubernetes.io/add-base-url: 'true'
    ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - http:
      paths:
      - path: /$WERCKER_APPLICATION_OWNER_NAME/user12/
        backend:
          serviceName: rest-jscreditscore-svc12
          servicePort: 8112
